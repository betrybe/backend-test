const DISPLAY_NAME = '"displayName" length must be at least 8 characters long';
const EMAIL = '"email" must be a valid email';
const PASSWORD_NAME = '"password" length must be 6 characters long';
const EMAIL_IS_REQUIRED = '"email" is required';
const PASSWORD_IS_REQUIRED = '"password" is required';

module.exports = {
  DISPLAY_NAME,
  EMAIL,
  EMAIL_IS_REQUIRED,
  PASSWORD_NAME,
  PASSWORD_IS_REQUIRED,
};
